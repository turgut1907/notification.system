global:
  resolve_timeout: 5m

route:
  receiver: "null"
  group_by: ["alertname"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: "null"

  # Example webhook receiver (uncomment and set route.receiver to use):
  # - name: "oncall"
  #   webhook_configs:
  #     - url: "http://example.com/alerts"
  #       send_resolved: true
